# Copy this file to .streamlit/secrets.toml (which is gitignored) and fill
# in your real key. On Streamlit Community Cloud, set this in the app's
# Settings > Secrets instead of committing a file.

GOOGLE_MAPS_API_KEY = "your-api-key-here"
